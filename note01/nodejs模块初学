模块
1.require 
require函数用于在当前模块中加载和使用别的模块，传入一个模块名，或模块文件的路径，返回一个模块导出对象。另外，模块名中的.js可以省略。
2.exports
exports对象是当前模块的导出对象，用于导出该模块中的部分公有方法或者属性。别的模块通过require函数使用当前模块导出的方法或属性，并且通过require函数得到的就是当前模块的exports对象。
3.module
通过modole对象可以访问当前模块的一些信息，但更多的用途是替换当前模块的导出对象。
